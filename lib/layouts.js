var blessed = require('blessed');

/**
 * A collection of options for widgits.
 *
 * Certain options need computation and therefore do not work with the current 
 * yaml configuration.
 */
module.exports = {};
